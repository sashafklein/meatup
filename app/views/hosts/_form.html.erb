<%= form_for(@host) do |f| %>
  <% if @host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :address %>
      <%= f.text_field :address %>
    </div>
    <div class="field">
      <%= f.label :city %>
      <%= f.text_field :city %>
    </div>
    <div class="field">
      <%= f.label :zip %>
      <%= f.text_field :zip, :value => current_user.zip %>
    </div>
    <div class="field">
      <%= f.label "Phone Number" %>
      <%= f.text_field :phone %>
    </div>
    <div class="field">
      <%= f.label "State" %>
      <%= f.select :state, us_states %>
    </div>
     <div class="actions">
    <%= f.submit @string, class: "btn image-button", style: "float:right;margin-top:15px;" %>
    </div>
<% end %>		