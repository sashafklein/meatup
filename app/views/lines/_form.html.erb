<%= form_for(@line) do |f| %>
  <% if @line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @line.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label "Units" %><br />
    <%= f.text_field :units %>
    <%= f.label "Notes" %><br />
    <%= f.text_field :notes %>
    <%= f.label "Order #" %><br />
    <%= f.text_field :order_id %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>		